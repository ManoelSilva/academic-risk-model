version: '3.8'

services:
  api:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - LOG_LEVEL=INFO
      - MODEL_PATH=models/production/model.joblib
    volumes:
      - ./models:/app/models
      - ./data:/app/data
    restart: unless-stopped
